{
  "version": 2,
  "builds": [
    {
      "src": "app.py",  // Change this to the name of your main Flask file if different
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
