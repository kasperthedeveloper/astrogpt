{
  "version": 2,
  "builds": [
    {
      "src": "app.py",  // Change this if your main Flask file is named differently
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
